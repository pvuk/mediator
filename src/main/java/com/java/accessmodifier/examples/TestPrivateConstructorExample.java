package com.java.accessmodifier.examples;

public class TestPrivateConstructorExample {
	
	public static void main(String[] args) {
		/*
		 * 1. constructor which is declared as private can't be accessed outside of class, it can be accessed inside of the class only.
		 * 2. We can't create new instance of class if the constructor is declared with private modifier.
		 */
//		PrivateConstructorExample privateConstructorExample = new PrivateConstructorExample();
	}
}
